export const TOOL_NAME = "auditpulse";

/**
 * Tool version reported in JSON and SARIF output. Kept here (not read from
 * package.json) so output stays stable regardless of how the CLI is bundled
 * or executed.
 */
export const TOOL_VERSION = "2.0.0";
