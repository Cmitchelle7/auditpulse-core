import { startServer } from "./server.js";

/**
 * Entry point for the AuditPulse web demo: `npm run dev:web`.
 * Kept separate from the CLI entry (src/index.ts) so both stay independent.
 */
startServer();
